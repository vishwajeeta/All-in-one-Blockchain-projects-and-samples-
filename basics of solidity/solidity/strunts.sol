//structs are custom defined types that can group several variables
// maximum number of variable inside struct is 16
//one can define one structs inside another struct in-order to increase the variables
 
 //mapping
 //Mapping is refered to hash
